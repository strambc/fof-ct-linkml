id: https://w3id.org/fof-ct/quality
name: fof_bas_ct_quality
description: >-
  FISH Omics Format for Chromatin Tracing - Ball-and-Stick modality (FOF-bas-CT).
  LinkML schema for the optional Spot Quality table (table 5). Stores quality
  metrics associated with individual DNA bright Spots (e.g. signal-to-noise
  ratio, localization uncertainty, fit quality). Indexed by Spot_ID. This
  table MUST contain at least one user-defined optional column describing a
  spot quality metric, declared via #^ header lines.
license: CC-BY-4.0
version: 1.0.0

prefixes:
  linkml: https://w3id.org/linkml/
  fof_ct: https://w3id.org/fof-ct/
  xsd: http://www.w3.org/2001/XMLSchema#

default_prefix: fof_ct
default_range: string

imports:
  - linkml:types
  - fof_bas_ct_common

# ---------------------------------------------------------------------------
# Classes
# ---------------------------------------------------------------------------

classes:

  SpotQualityRecord:
    description: >-
      A single row in the Spot Quality table. Each instance captures one or
      more quality metrics for a specific DNA bright Spot identified by
      Spot_ID. At least one user-defined quality metric column MUST be
      present; users declare these via #^ header lines.
    slots:
      - spot_id
      - channel_name
      - fluorophore_name
      - x_precision
      - y_precision
      - z_precision
      - photon_count
      - goodness_of_fit
      - centroid_intensity
      - peak_intensity
      - raw_x
      - raw_y
      - raw_z
      - x_drift
      - y_drift
      - z_drift
      - x_chromatic_shift
      - y_chromatic_shift
      - z_chromatic_shift
      - x_loc_error
      - y_loc_error
      - z_loc_error
    slot_usage:
      spot_id:
        required: true
        identifier: true
        description: >-
          Unique integer identifier for the DNA bright Spot to which these
          quality metrics belong. Links to the corresponding Spot record in
          the core table (table 1). Must be unique within this table.
      channel_name:
        required: true
      fluorophore_name:
        required: true
      x_precision:
        required: false
        description: "Recommended: metric for X localization precision"
      y_precision:
        required: false
      z_precision:
        required: false
      photon_count:
        required: false
        description: "Optional standardised name for photon count"
      goodness_of_fit:
        required: false
      centroid_intensity:
        required: false
      peak_intensity:
        required: false
      raw_x:
        required: false
      raw_y:
        required: false
      raw_z:
        required: false
      x_drift:
        required: false
      y_drift:
        required: false
      z_drift:
        required: false
      x_chromatic_shift:
        required: false
      y_chromatic_shift:
        required: false
      z_chromatic_shift:
        required: false
      x_loc_error:
        required: false
      y_loc_error:
        required: false
      z_loc_error:
        required: false

  SpotQualityTable:
    description: >-
      The Spot Quality table of a FOF-bas-CT dataset (namespace:
      4dn_FOF-CT_quality). This class represents the entire file: it holds
      all dataset-level provenance metadata (recorded as header lines in the
      TSV serialisation) together with the full collection of SpotQualityRecord
      rows. Submission of this table is optional but recommended.
    tree_root: true
    slots:
      # --- mandatory header fields ---
      - fof_ct_version
      - table_namespace
      - lab_name
      - experimenter_name
      - experimenter_contact
      - description
      - additional_tables
      # --- conditional header fields ---
      - softwares
      - xyz_unit
      - time_unit
      - intensity_unit
      - intensity_measurement_method
      # --- data rows ---
      - spot_quality_records
    slot_usage:
      fof_ct_version:
        required: true
      table_namespace:
        required: true
        equals_string: 4dn_FOF-CT_quality
        description: >-
          Identifier for this table type. Must always be '4dn_FOF-CT_quality'.
          Written as ##Table_Namespace= in the file header.
      lab_name:
        required: true
      experimenter_name:
        required: true
      experimenter_contact:
        required: true
      description:
        required: true
      additional_tables:
        required: true
      softwares:
        required: false
        description: >-
          One or more Software entries documenting every tool used to produce
          or process data in this table. Required only when software was used;
          omit the block entirely if no software was applied. Written as
          repeating #Software_* blocks in the file header.
      xyz_unit:
        required: false
        description: >-
          Unit used for any spatial coordinate or distance metric reported in
          user-defined columns. Conditionally required when any such metric
          is present. Written as ##XYZ_Unit= in the file header.
      time_unit:
        required: false
        description: >-
          Unit used for any time metric reported in user-defined columns.
          Conditionally required when any such metric is present. Written as
          ##Time_Unit= in the file header.
      intensity_unit:
        required: false
        description: >-
          Unit used for any intensity metric reported in user-defined columns.
          Conditionally required when any such metric is present. Written as
          ##Intensity_Unit= in the file header.
      intensity_measurement_method:
        required: false
        description: >-
          Method used to perform intensity measurements. Conditionally required
          when any intensity metric is present. Written as
          #Intensity_Measurement_Method: in the file header.
      spot_quality_records:
        required: true

# ---------------------------------------------------------------------------
# Slots local to this schema
# ---------------------------------------------------------------------------

slots:

  spot_quality_records:
    description: >-
      The complete collection of SpotQualityRecord rows constituting this
      dataset. Each record corresponds to one data row in the TSV
      serialisation and must include at least one user-defined quality
      metric column.
    domain: SpotQualityTable
    range: SpotQualityRecord
    multivalued: true
    inlined_as_list: true

  channel_name:
    description: >-
      The name of the imaging channel used for this Spot (e.g. 510/25).
      Mandatory in the Spot Quality table.
    range: string
    examples:
      - value: "510/25"
      - value: "647/50"

  fluorophore_name:
    description: >-
      The name of the fluorophore used for this Spot (e.g. AlexaFluor_488).
      Mandatory in the Spot Quality table.
    range: string
    examples:
      - value: AlexaFluor_488
      - value: Cy5

  centroid_intensity:
    description: >-
      Signal intensity of the centroid pixel of the Spot.
      Conditionally required in the Spot Quality table when intensity
      metrics are reported.
    range: float
    examples:
      - value: "2500.0"

  peak_intensity:
    description: >-
      Signal intensity of the brightest pixel within the Spot boundary.
      Conditionally required in the Spot Quality table when intensity
      metrics are reported.
    range: float
    examples:
      - value: "3200.0"

  raw_x:
    description: >-
      X coordinate of this Spot before any post-processing corrections
      (drift correction, chromatic correction, etc.). Same unit as X.
    range: float
    examples:
      - value: "14.30"

  raw_y:
    description: >-
      Y coordinate of this Spot before any post-processing corrections.
      Same unit as Y.
    range: float
    examples:
      - value: "41.20"

  raw_z:
    description: >-
      Z coordinate of this Spot before any post-processing corrections.
      Same unit as Z.
    range: float
    examples:
      - value: "1.10"

  x_drift:
    description: >-
      Drift correction offset applied to the X coordinate of this Spot.
      Same unit as X.
    range: float
    examples:
      - value: "0.13"

  y_drift:
    description: >-
      Drift correction offset applied to the Y coordinate of this Spot.
      Same unit as Y.
    range: float
    examples:
      - value: "0.23"

  z_drift:
    description: >-
      Drift correction offset applied to the Z coordinate of this Spot.
      Same unit as Z.
    range: float
    examples:
      - value: "0.13"

  x_chromatic_shift:
    description: >-
      Chromatic aberration correction offset applied to the X coordinate
      of this Spot. Same unit as X.
    range: float
    examples:
      - value: "0.05"

  y_chromatic_shift:
    description: >-
      Chromatic aberration correction offset applied to the Y coordinate
      of this Spot. Same unit as Y.
    range: float
    examples:
      - value: "0.04"

  z_chromatic_shift:
    description: >-
      Chromatic aberration correction offset applied to the Z coordinate
      of this Spot. Same unit as Z.
    range: float
    examples:
      - value: "0.02"

  x_loc_error:
    description: >-
      Localization error estimate for the X coordinate of this Spot
      (e.g. standard deviation of repeated measurements). Same unit as X.
    range: float
    examples:
      - value: "0.02"

  y_loc_error:
    description: >-
      Localization error estimate for the Y coordinate of this Spot.
      Same unit as Y.
    range: float
    examples:
      - value: "0.02"

  z_loc_error:
    description: >-
      Localization error estimate for the Z coordinate of this Spot.
      Same unit as Z.
    range: float
    examples:
      - value: "0.05"
